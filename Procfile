# Procfile — OIL Benchmarks
#
# Used by Heroku, Dokku, Railway, Render, and other PaaS platforms that
# follow the 12-factor app convention.
#
# The platform's process manager handles restarts, scaling, and log
# aggregation automatically. The app already listens on process.env.PORT.
#
# Dyno types:
#   web   — the main Express server (the only mandatory process)

web: node server/index.js
